on: 
  pull_request:
    types: [synchronize]
  push:
    branches:    
      - main
  issue_comment:                                     
    types: [created, edited]
name: Scan Secret (go/secret-scanner)
jobs:
  scan_secret:
    name: Scan incoming changes
    runs-on: ubuntu-latest
    if: ${{ !github.event.issue.pull_request }}
    steps:
      - name: Run scanner
        run: |
          echo ${{ github.sha }}
  verify_allow_list:
    name: Verify allow list
    runs-on: ubuntu-latest
    if: github.event.issue.pull_request && contains(github.event.comment.body, '/verify')
    steps:
      - name: debug
        run: |
          echo ${{ github.event.issue.pull_request }}
      - name: Checkout PR branch
        run: | 
          echo ${{ github.sha }}
      # - name: Run scanner in verification mode
      #   uses: aristanetworks/secret-scanner-service@beta
      #   with:
      #     mode: verification
